# Tool Capabilities

Tools are defined by capabilities and requirements, not by hard-coded executable names.

Concrete tool mappings belong in private local state unless the mapping is a safe repository-wide recommendation.

