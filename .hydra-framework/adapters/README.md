# Adapters

Adapters isolate provider-specific, lifecycle-specific, and runtime-specific behavior from the shared core.

Common contracts belong here. Private executable mappings and credentials belong in `.hydra-framework.local/`.

